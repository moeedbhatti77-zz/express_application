extends layout

block content
  .row 
    //- .col-sm-8 
    //-   h1 Faculty
    .col-sm-4 
      a(href="/add", class="btn btn-primary float-right" ) Add New Faculty
  table.table.table-bordered.table-hover
    thead
      tr
        th Title 
        th Price
    tbody 
      each product in products 
        tr 
          td=product.title 
          td=product.price